// Variável
let numero = 7;

// Verifica se o número é positivo
const numeroEhPositivo = numero > 0;

// Estrutura Condicional: IF, ELSE IF, ELSE
if (numero === 0) {
   console.log("Neutro");
} else if (numeroEhPositivo) {
  console.log("Positivo");
} else {
  console.log("Negativo");
}